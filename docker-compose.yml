services:
  relay:
    build: .
    ports:
      - "${PORT:-7777}:7777"
    volumes:
      - relay-data:/data
    environment:
      - PORT=7777
      - DB_PATH=/data/relay.db
      - MAX_SUBS=20
      - MAX_FILTERS=10
      - MAX_EVENT_TAGS=2500
    restart: unless-stopped

volumes:
  relay-data:
